sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t smoosmoo/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true smoosmoo/react-test npm test

after_success:
  - docker build -t smoosmoo/multi-client ./client
  - docker build -t smoosmoo/multi-nginx ./nginx
  - docker build -t smoosmoo/multi-server ./server
  - docker build -t smoosmoo/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push smoosmoo/multi-client
  - docker push smoosmoo/multi-nginx
  - docker push smoosmoo/multi-server
  - docker push smoosmoo/multi-worker
